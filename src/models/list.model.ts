// (key, value ) >> seam what we did at mockAPI >> this model file for data type
// parent
export interface List {
  id: string;
  title: string;
  description: string;
  createdAt: Date;
  updatedAt: Date;
}
